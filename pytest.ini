[pytest]
testpaths =
    tests/web
    tests/api
    tests/services
    tests/printing
python_files = test_*.py
addopts = -q --cov=app --cov-config=pyproject.toml --cov-report=term-missing --cov-fail-under=50

